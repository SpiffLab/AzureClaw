"""Azure-specific helpers used across the AzureClaw package.

This subpackage holds adapters for Azure SDKs (identity, Key Vault, Service
Bus, Blob Storage, App Insights, etc.). Each adapter is intentionally small
and is introduced by the OpenSpec change that first needs it.
"""
